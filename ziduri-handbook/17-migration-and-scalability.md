# 17 — Migration and Scalability
Static → Astro when many products share templates, metadata generation becomes repetitive, or content collections become useful.

Static → Next.js only when SSR or substantial dynamic/authenticated functionality is actually required. Do not migrate merely for SEO.

Keep product metadata structured and portable. Preserve public URLs during every migration.
